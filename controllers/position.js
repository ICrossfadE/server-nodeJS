const getById = function(req, res) {
  res.status(200).json({login: true});
}

const remove = function(req, res) {
  res.status(200).json({login: true});
}

const create = function(req, res) {
  res.status(200).json({login: true});
}

const update = function(req, res) {
  res.status(200).json({login: true});
}

module.exports = {getById, remove, create, update}